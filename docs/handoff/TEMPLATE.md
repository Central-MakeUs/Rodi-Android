# HANDOFF — Current Task

Status:
Task:
Branch:
Base:
Risk:

## Context

## Goal

## Spec

## 재현 경로 (필수)

<!--
버그 수정이면 **어떤 조작으로 증상이 나오는지** 정확히 적는다. 여기가 비면 구현자도 리뷰어도
자기 편한 경로로 확인하고 "통과"라고 보고하게 된다.

실제 사고(2026-08-14): 증상은 "바텀시트 핸들을 잡고 드래그해서 내릴 때" 나오는데, 스펙에 경로가
없어서 리뷰어가 **뒤로가기 버튼으로만** 5회 검증하고 APPROVE했다. 버그가 사는 경로를 한 번도
밟지 않은 검증이었다.

- 진입: (예) 홈 → 목록열기 → "일산 시내 순환 코스" 선택
- 트리거 조작: (예) 시트 상단 핸들을 손가락으로 잡고 아래로 천천히 드래그
- 기대 결과 / 실제 결과:
- 이 경로로 재현이 안 되면 그 사실 자체를 보고하고 멈춘다(추측 수정 금지).
-->

## 회귀 금지 목록 (invariants)

<!--
이번 변경으로 **바뀌면 안 되는 것**을 구체적으로 적는다. "하지 마라"가 아니라 검증 가능한 문장으로.
잘 작동한 사례: "겹치지 않는 일반 코스는 마커 위치가 1px도 달라지면 안 된다" → 구현자가 정확히
지켰고 비순환 코스 회귀가 없었다.

이 프로젝트는 단위 테스트 76개 대비 계측/스크린샷 테스트가 사실상 없다(2026-08-14 기준).
즉 UI·제스처·지도 렌더링 회귀는 **자동으로 안 잡힌다.** `./gradlew test` 통과는 이 영역에서
"검증됨"의 근거가 되지 못하므로, 여기에 사람이 직접 확인할 항목을 남겨야 한다.

- [ ] (예) 비순환 코스 마커 위치·모양 변화 없음
- [ ] (예) 시트 접힘/펼침 높이와 스크롤 동작 변화 없음
-->

## Acceptance

<!--
각 항목은 **무엇을 보면 통과인지**가 드러나야 한다. "정상 동작 확인" 같은 문장은 금지.
-->

## Expected Files

## Verification

### 증거 규칙 (필수)

<!--
"N회 확인했다"는 주장은 반증 가능한 형태로만 인정한다.

실제 사고(2026-08-14): 구현자가 "6회 반복 확인"이라며 스크린샷 6장을 제출했으나, md5를 떠보니
`cycle-1~6-map.png` 6개가 **전부 동일 파일**이었고 나머지도 6개 중 4개만 고유했다. 반복 자체가
없었다. 아래 규칙은 그걸 자동으로 걸러내려고 있다.

- 회차별 캡처는 **md5가 전부 달라야** 한다. 확인 명령을 결과에 함께 붙인다:
  `md5 -q shot1.png shot2.png ... | sort | uniq -d`  → 출력이 있으면 중복(=반복 안 함)
- 캡처에 타임스탬프(상태바 시계)가 보이면 회차마다 진행해야 한다.
- 성능/잼 관련은 눈이 아니라 수치로: `adb shell dumpsys gfxinfo com.dororong.rodi` 의
  janky frame 비율을 **수정 전/후 둘 다** 측정해 붙인다.
- 기기 확인이 불가능했다면 "확인 못 함"이라고 쓴다. 이는 nit이 아니라 **Blocking**이다.
-->

## Out of Scope

## Implementation Result

## Review

## Review Triage

## Revision Plan

## Revision Result

## Final Review
